
public class search_Tree {
	
	node root;

	
	private node addRecursive(node current, IMF) {
		
		if(current==null)
			return new	node();
		
		if(current.transition=="left") {
			current.left=addRecursive(current.left,value);	
		}	
		
	}
	
	private boolean compare() {
		
	}
}
